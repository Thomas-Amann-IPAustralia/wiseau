# Web framework
fastapi
uvicorn[standard]
python-multipart          # multipart/form-data uploads for /convert/file

# Rate limiting (fair use on shared free compute)
slowapi

# URL extraction
selenium
selenium-stealth
trafilatura
markdownify

# Document parsing
pymupdf4llm               # PDF -> Markdown (pulls in pymupdf/fitz)
mammoth                   # DOCX -> HTML
